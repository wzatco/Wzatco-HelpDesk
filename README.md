# AdminNAgent — Local dev

This workspace contains the AdminNAgent project blueprint. A minimal Next.js app was added so you can begin building the Admin Panel Live Chat feature.

How to run locally (PowerShell):

```powershell
npm install
npm run dev
```

Visit http://localhost:3000
